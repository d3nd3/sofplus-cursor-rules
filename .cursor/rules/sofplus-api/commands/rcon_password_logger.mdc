---
description: rcon_password_logger - Rcon password used by the SoF logger program. Don't use the same password for rcon_password. When the dumpuser, serverinfo and status commands are executed, no information will be displayed in the console.
alwaysApply: false
---

### rcon_password_logger

rcon_password_logger

Rcon password used by the SoF logger program. Don't use the same password for rcon_password. When the dumpuser, serverinfo and status commands are executed, no information will be displayed in the console.

Values:

- Empty: Disabled (default)
- Not empty: Enabled
