---
description: sp_sv_client_blue - Force client into the blue team. The `team_red_blue` cvar will be set.
alwaysApply: false
---

### sp_sv_client_blue

sp_sv_client_blue

Force client into the blue team. The `team_red_blue` cvar will be set.

Synopsis:

```txt
sp_sv_client_blue
SLOT|*
```
Parameters:

- SLOT|*:
