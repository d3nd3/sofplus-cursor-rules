---
description: sp_sv_client_spec - Force client into spectator mode. The `spectator` and `spectator_password` cvars will be set.
alwaysApply: false
---

### sp_sv_client_spec

sp_sv_client_spec

Force client into spectator mode. The `spectator` and `spectator_password` cvars will be set.

Synopsis:

```txt
sp_sv_client_spec
SLOT|*
```
Parameters:

- SLOT|*:
