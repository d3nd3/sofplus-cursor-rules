---
description: sp_sv_client_swap - Force client into the red or blue team. The `team_red_blue` cvar will be set.
alwaysApply: false
---

### sp_sv_client_swap

sp_sv_client_swap

Force client into the red or blue team. The `team_red_blue` cvar will be set.

Synopsis:

```txt
sp_sv_client_swap
SLOT|*
```
Parameters:

- SLOT|*:
