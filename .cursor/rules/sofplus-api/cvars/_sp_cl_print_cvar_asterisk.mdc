---
description: _sp_cl_print_cvar_asterisk - _sp_cl_print_cvar_*
alwaysApply: false
---

### _sp_cl_print_cvar_asterisk

_sp_cl_print_cvar_*

Rcon cvar get result. When parsing the result from a `rcon
CVAR NAME
` command, the result will be stored in `_sp_cl_print_cvar_
CVAR NAME
`.

Example:

Enable print message parsing and get a cvar:

`]set _sp_cl_print 1

                          ]rcon timelimit`

Wait a bit to let the server respond...

`]echo #_sp_cl_print_cvar_timelimit

                          20`
