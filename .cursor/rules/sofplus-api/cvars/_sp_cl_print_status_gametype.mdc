---
description: _sp_cl_print_status_gametype - Rcon status result. When parsing the response to a `rcon status` command, the gametype will be stored in this variable.
alwaysApply: false
---

### _sp_cl_print_status_gametype

_sp_cl_print_status_gametype

Rcon status result. When parsing the response to a `rcon status` command, the gametype will be stored in this variable.
