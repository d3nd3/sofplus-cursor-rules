---
description: _sp_cl_print_status_map - Rcon status result. When parsing the response to a `rcon status` command, the map name will be stored in this variable.
alwaysApply: false
---

### _sp_cl_print_status_map

_sp_cl_print_status_map

Rcon status result. When parsing the response to a `rcon status` command, the map name will be stored in this variable.
